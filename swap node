 ListNode* temp=head,*temp1;
        
        while(temp!=0&&temp->next!=0)
        {
            temp1=temp->next;
            if(temp1)
            {
                swap(temp->val,temp1->val);
                
            }
            
            temp=temp1->next;
            
        }
        return head;
